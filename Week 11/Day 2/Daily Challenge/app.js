// app.js

// Require the colorful-message.js module
const displayColorfulMessage = require('./colorful-message');

// Call the function to display the colorful message
displayColorfulMessage().catch(err => console.error(err));
